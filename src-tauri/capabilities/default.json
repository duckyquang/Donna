{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main Donna window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
