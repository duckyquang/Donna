import { PageShell, Placeholder } from "../components/PageShell";

export default function Calendar() {
  return (
    <PageShell
      title="Calendar"
      subtitle="Your schedule, with two-way Google Calendar sync."
    >
      <Placeholder note="Calendar view with Google Calendar sync arrives in Phase 2." />
    </PageShell>
  );
}
